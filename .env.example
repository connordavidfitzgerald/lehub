PUBLIC_SANITY_PROJECT_ID=e797khcc
PUBLIC_SANITY_DATASET=production
# Only needed to run scripts/seed.mjs — the site build reads published content
# from the public dataset and needs no token.
SANITY_WRITE_TOKEN=
